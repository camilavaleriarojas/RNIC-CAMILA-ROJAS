import React from 'react';
import {Container, Title} from './styles';

const Header = () => {
  return (
    <Container>
      <Title>My Tasks</Title>
    </Container>
  );
};

export default Header;
